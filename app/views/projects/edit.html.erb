<section class="bg-white dark:bg-gray-700">
  <div class="py-8 px-4 mx-auto max-w-screen-xl sm:py-16 lg:px-6">
    <h2 class="mb-8 text-4xl tracking-tight font-extrabold text-gray-900 dark:text-white">Edit "<%= @project.title %>" project</h2>
    <div class="mb-10">
      <h3 class="flex items-center mb-4 text-lg font-medium text-gray-900 dark:text-white">
        Project status:  <%= @project.status %>
      </h3>
      <p class="text-gray-500 dark:text-gray-400"><%= @project.description %></p>
    </div>
  </div>
</section>

<%= render "form", project: @project %>
